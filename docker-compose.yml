services:
  backend:
    build: ./backend
    environment:
      FRAMEFLOW_DATABASE_PATH: /app/data/frameflow.db
      FRAMEFLOW_API_TOKEN: ${FRAMEFLOW_API_TOKEN:-}
      FRAMEFLOW_ADDRESS: 0.0.0.0:28741
    volumes: [frameflow-data:/app/data]
    ports: ["28741:28741"]
    healthcheck: {test: ["CMD", "wget", "-qO-", "http://127.0.0.1:28741/health"], interval: 10s, timeout: 3s, retries: 5}
  frontend:
    build: ./frontend
    depends_on: {backend: {condition: service_healthy}}
    ports: ["8088:80"]
volumes: {frameflow-data: {}}
